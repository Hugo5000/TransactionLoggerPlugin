database:
  type: sqlite                    # the type of database mysql or sqlite
  table-prefix: transaction_logger_   # the prefix for the database tables default: storage_vaults_
  # everything needed for mysql to work:
  username:
  password:
  database:
  ip:
  port: